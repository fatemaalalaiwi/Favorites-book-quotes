<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Book</title>
  <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>

  <%- include('../partials/_navbar.ejs') %>

  <div class="form-container">
    <h1>Edit Book ✏️</h1>

    <form action="/books/<%= book._id %>?_method=PUT" method="POST" enctype="multipart/form-data">

      <div class="form-group">
        <label for="BookName">📘 Book Name</label>
        <input type="text" name="BookName" id="BookName" value="<%= book.BookName %>" required />
      </div>

      <div class="form-group">
        <label for="Author">✍️ Author</label>
        <input type="text" name="Author" id="Author" value="<%= book.Author %>" required />
      </div>

      <div class="form-group">
        <label>📸 Current Book Image</label><br>
        <% if (book.bookImage) { %>
          <img src="/<%= book.bookImage %>" alt="Book Image" style="max-width: 200px; border-radius: 15px; margin-top: 10px;" />
        <% } else { %>
          <p>No image uploaded.</p>
        <% } %>
      </div>

      <div class="form-group">
        <label for="bookImage">Upload New Image</label>
        <input type="file" name="bookImage" id="bookImage" accept="image/*" />
      </div>

      <button type="submit" class="submit-btn">Save Changes</button>
      
      <a href="/books/<%= book._id %>" class="cancel-btn">Cancel</a>

    </form>
  </div>

</body>
</html>