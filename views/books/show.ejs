<body>
  <%- include('../partials/_navbar.ejs') %>

  <!-- display All Quotes -->
     
  <h2>Quotes</h2>
  <h1> <%= myBook.bookQuotes %></h1>
  <p> Add by: $<%= myBook.owner.username  %></p>


    <br>

<% if(userHasFavorited) { %>
    <p>You like this quote!</p>
    <form action="/books/<%= myBook._id %>/favorited-by/<%= user._id %>?_method=DELETE" method="POST">
      <button type="submit">dont like !</button>
    </form>
  <% } else { %>
    <p>You dont like this quote!</p>
    <form action="/books/<%= myBook._id %>/favorited-by/<%= user._id %>" method="POST">
        <button type="submit">Like!</button>
      </form>
  <% } %>


<p> This quote liked by <%= myBook.favoritedByUser.length %> people.</p>


  <h2>Authorization</h2>
  <% if (myBook.owner._id.equals(user._id)) { %>
    <p>You can update and delete</p>
    <a href="/books/<%= myBook._id %>/edit">Edit this page</a>
    <form action="/books/<%= myBook._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete this page</button>
    </form>
    
  <% } else { %>
    <p>This is not your quote, only <%= myBook.owner.username %>  can edit and delete!</p>
  <% } %>
</body>






